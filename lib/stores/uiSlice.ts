import { StateCreator } from 'zustand';
import { TunrStore, UISlice } from './types';

export const createUISlice: StateCreator<TunrStore, [], [], UISlice> = (set, get) => ({
    // Future UI state here
});
